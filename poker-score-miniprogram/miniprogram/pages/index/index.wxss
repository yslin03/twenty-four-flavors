.hero {
  padding: 52rpx 12rpx 46rpx;
  text-align: left;
}
.index-page { position: relative; }
.hero-badge {
  font-size: 26rpx;
  color: var(--accent);
  letter-spacing: 0;
  opacity: 0.9;
  margin-bottom: 20rpx;
}
.hero-title {
  font-size: 62rpx;
  font-weight: 700;
  letter-spacing: -2rpx;
}
.hero-sub {
  margin-top: 16rpx;
  font-size: 30rpx;
  color: var(--muted);
}
.actions {
  display: flex;
  flex-direction: column;
}
.join-row {
  display: flex;
  margin-top: 24rpx;
}
.join-input {
  flex: 1;
}
.join-btn {
  width: 200rpx;
  height: 88rpx;
  line-height: 88rpx;
  margin-left: 20rpx;
}
.demo-btn {
  margin-top: 24rpx;
}
.scan-btn {
  margin-top: 24rpx;
}
.career {
  margin-top: 24rpx;
}
.career-row {
  display: flex;
}
.career-item {
  flex: 1;
  text-align: center;
}
.career-num {
  font-size: 40rpx;
  font-weight: 800;
  font-variant-numeric: tabular-nums;
}
.career-label {
  margin-top: 6rpx;
  font-size: 24rpx;
  color: var(--muted);
}
.career-rank {
  margin-top: 20rpx;
  font-size: 24rpx;
  color: var(--accent);
  text-align: center;
}
